import { Button, Form, Navbar, Container, Nav, NavDropdown, Card, ButtonGroup, Table} from 'react-bootstrap';
import { useState } from 'react';

export default function basketball_template() {
    const [overall, setOverall] = useState("")

    return (
        <>
            <tr></tr>
        </>
    );
}